/* e01: 变量与类型 */
#include <stdio.h>

int main(void)
{
    int    a = 5;          // 整型
    float  f = 1.5f;       // 浮点
    char   c = 'A';        // 字符
    unsigned int u = 3000; // 无符号整型（电机电流原始值常用它）

    // TODO: 打印 a、f、c、u
    // 提示: printf("a=%d f=%.1f c=%c u=%u\n", a, f, c, u);
    return 0;
}
