/* e06: 数组 —— CAN 帧数据就是 8 个字节 */
#include <stdio.h>

int main(void)
{
    unsigned char data[8] = {0};  // 8 个字节，初始全 0

    // TODO: 把 data[0] 设成 1, data[7] 设成 2，然后打印 data[0] 和 data[7]
    return 0;
}
