/* e03: for/while 循环，算 1+2+...+100 */
#include <stdio.h>

int main(void)
{
    int sum = 0;
    int i;

    // TODO: 用 for 循环从 1 加到 100，结果放 sum
    // (bench 里每 1ms 循环一次的 while(1) 思路和这类似)

    printf("1+2+...+100 = %d\n", sum);
    return 0;
}
